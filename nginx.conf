server {
    listen 80;
    server_name localhost;

    # Serve HTML files
    location / {
        root /home/dev/html;
        index index.html;
    }

    # Serve Markdown slides
    location /slides/ {
        root /home/dev;
    }

    error_page 404 /404.html;
}
